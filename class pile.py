class Pile:
    
    def __init__(self, taille_max):
        self.taille_max = taille_max
        self.contenu = [None] * taille_max
        self.taille = 0
    
    def est_vide(self):
        if taille == 0:
            return True
        else:
            return False
   
    def empiler(self, s):
        if taille_max > taille:
            s = self.taille + 1
    
    def depiler(self):
         if taille_max < taille:
             self.taille - 1
             
    def taille_pile(self):
        return self.taille
    
            
    
        
        
        
        
        
    